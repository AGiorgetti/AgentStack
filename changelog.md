# Changelog

## [Unreleased]

- No changes yet.

## [0.1.0]

- Initial release: core CLI (`src/`), protocol assets (`assets/modules/protocol/`), documentation (`docs/`), and test suite (`tests/`).
